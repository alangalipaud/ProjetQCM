{% extends '::base.html.twig' %}

{% block body -%}
    <div class="col-md-12">
        <h1>Question list</h1>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Wording</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('question_show', { 'id': entity.id }) }}">{{ entity.wording }}</a></td>
                    <td>
                    <ul>
                        <a href="{{ path('question_show', { 'id': entity.id }) }}">
                            <button type="button" class="btn btn-info no-margin">
                                Show
                                <span class="glyphicon glyphicon-align-left" aria-hidden="true">
                                </span>
                            </button>
                        </a>
                        <a href="{{ path('question_edit', { 'id': entity.id }) }}">
                            <button type="button" class="btn btn-success no-margin">
                                Edit
                                <span class="glyphicon glyphicon-edit" aria-hidden="true">
                                </span>
                            </button>
                        </a>
                    </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <a href="{{ path('question_new') }}">
            <button type="button" class="btn btn-success no-margin">
                Create
            <span class="glyphicon glyphicon-plus" aria-hidden="true">
            </span>
            </button>
        </a>
    </div>
{% endblock %}
