{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Registration list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Startdate</th>
                <th>Enddate</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('registration_show', { 'id': entity.id }) }}">{{ entity.startdate }}</a></td>
                <td>{% if entity.enddate %}{{ entity.enddate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('registration_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('registration_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('registration_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
